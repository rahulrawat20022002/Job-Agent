# Re-hydration prompt — paste this into a NEW chat to resume Hirschmann interview prep

Copy everything in the code block below into a fresh chat, and make sure this folder
(jobSearchClaude/interview_prep) is connected so the assistant can read the STATE file.

```
I'm continuing my interview prep for the Hirschmann/Belden "Agentic Pentesting" Masterarbeit
(interview Mon 15 Sep 2026, 13:30, remote, live case study). Read the single source of truth
first: jobSearchClaude/interview_prep/Hirschmann_Belden_Agentic_Pentesting_STATE.md — treat it
as complete and authoritative, and keep it updated on disk as we go.

Status: the live-path agent walkthrough (PolicyNeural / repo Data_analytics_2, Agents 1–9) is
DONE. My PolicyNeural code is public and re-fetchable via
raw.githubusercontent.com/rahulrawat20022002/Data_analytics_2/main/src/agents/<agent>.py — ALWAYS
fetch the real code + verify the orchestrator call-site before explaining anything; never work
from memory.

Follow the mandatory per-agent WALKTHROUGH FORMAT spelled out at the top of §5 of the STATE file
for any agent write-up (one-line frame → Job → mental model → the key code (yours) → walkthrough
→ call-site (verified) → own it → soundbite → plain recap), and after each agent update DONE /
move NEXT / bump the date.

Pick up from whatever the §5 NEXT pointer says. Also check §7 open threads (commit the
debate_agent.py prompt fix, then one real orchestrator test-run).

Style: blunt and direct, honest trade-offs, no corporate filler; end every wrap-up with a plain,
jargon-free recap.
```
